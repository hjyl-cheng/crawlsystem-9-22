apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: crawler-pg-daily
  namespace: db
spec:
  schedule: 0 0 3 * * *
  backupOwnerReference: self
  cluster:
    name: crawler-pg
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
